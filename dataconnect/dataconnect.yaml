specVersion: "v1"
serviceId: "gstore"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gstore-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
